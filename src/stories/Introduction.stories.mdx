import { Meta } from '@storybook/addon-docs/blocks'
import * as ui from './ui'

<Meta title="Infancy/Introduction" />

# Infancy

Infancy is a UI toolkit that doesn’t have an implementation.

## infancy.Button

A button takes up a full width.

<ui.BasicButton onClick={() => alert('Ouch!')}>Hello!</ui.BasicButton>

## infancy.Panel

A panel has a title, topBar, children, and bottomBar.

<ui.BasicPanel title="A panel">This is the panel’s body.</ui.BasicPanel>

<ui.BasicPanel
  title="A panel with top bar and bottom bar"
  topBar={<>Top bar</>}
  bottomBar={<>Bottom bar</>}
>
  Normally, you would put a buttons in the top bar and bottom bar.
</ui.BasicPanel>
